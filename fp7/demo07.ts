// /**
//  * 函数最后一条语句是下一次递归（即处于尾部）
//  * @param n
//  * @param current
//  */
// const factorial = (n, current = 1) =>
//   n === 1 ? current : factorial(n - 1, n * current);

// const factorial = (n, current = 1) =>
//   n === 1 ? current : factorial(n - 1, n * current);
